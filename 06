#include <stdio.h>

int easy(int c)
{
	int s = 0;
	for(int i = 2; i<(c/2)+1; i++)
	{
		if(c % i == 0)
		{
			s++;
		}
	}
	if (s==0) return 1;
	else return 0;
}
int main(void) {
	int n;
	scanf("%d", &n);
	for (int i = 2; i < n+1; i++)
	{
		if(easy(i) == 1)
		{
			if(n%i == 0) printf("%d ", i);
		}
	}
	return 0;
}
