#include <stdio.h>
#include <math.h>

int easy(int c)
{
	int s = 0;
	for(int i = 2; i<sqrt(c)+1; i++)
	{
		if(c % i == 0)
		{
			s++;
		}
	}
	if (s==0 || c==2) return 1;
	else return 0;
}
int main(void) {
	int n;
	scanf("%d", &n);
	for (int i = 2; i < sqrt(n)+1; i++)
	{
		if(easy(i) == 1)
		{
			if (i == sqrt(n)) printf("%d", i);
			else if(n%i == 0)
			{
				printf("%d ", i);
				if (easy(n/i) == 1) printf("%d ", n/i);
			}
		}
	}
	return 0;
}
