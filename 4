#include <stdio.h>

int main(void) {
	int n;
	scanf("%d", &n);
	int a = 0;
	for(int i = 2; i<(n/2)+1; i++)
	{
		if(n%i == 0)
		{
			printf("Число %d делится на %d. \n", n, i);
			a++;
		}
	}
	if(a==0)
	{
		printf("Число %d простое", n);
	}
	return 0;
}
