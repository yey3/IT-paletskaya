#include <stdio.h>

int main(void) {
	int N;
	scanf("%d", &N);
	double n[N];
	for (int i = 0; i<N; i++)
	{
		scanf("%lf", &n[i]);
	}
	double max = n[0];
	for (int i = 1; i<N; i++)
	{
		if(max<n[i]) max = n[i];
	}
	printf("%lf", max);
	return 0;
}
