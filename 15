#include <stdio.h>

int main(void) {
	int N;
	scanf("%d", &N);
	if(N%400==0) printf("Год %d високосный", N);
	else if (N%100==0) printf("Год %d невисокосный", N);
	else if (N%4==0) printf("Год %d високосный", N);
	else printf("Год %d невисокосный", N);
	return 0;
}
