#include <stdio.h>

int main(void) {
	for (int i = 1; i<101; i++)
	{
		int a = i%3;
		int b = i%5;
		if (a == 0) printf("FIZZ");
		if (b == 0) printf("BUZZ");
		if (a!=0 && b!=0) printf("%d", i);
		printf(" ");
	}
	return 0;
}
