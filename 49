#include <stdio.h>

int main(void) {
	int N;
	scanf("%d", &N);
	double mas[N];
	for (int i = 0; i < N; i++)
	{
		scanf("%lf", &mas[i]);
	}
	int rez = 0;
	for (int i = 1; i < N; i++)
	{
		if (mas[i] < mas[i-1]) rez++;
	}
	printf("%d", rez);
	return 0;
}
