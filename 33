#include <stdio.h>

int main(void) {
	int N;
	scanf("%d ", &N);
	int M = N;
	for(int i = 0; i < N + 1; i++)
	{
		for (int a = i; a < M + 1; a++)
		{
			printf("%d ", a);
		}
		M++;
		printf("\n");
	}
	return 0;
}
